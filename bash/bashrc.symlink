# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions
alias ll='ls -lh --color --time-style=long-iso --group-directories-first'

# If tmux exists, let's attach last session of tmux or start a new one
which tmux > /dev/null && [[ -z "$TMUX" ]] && (tmux attach || tmux)

export PS1='\n[\t] \e[32m\u@\H\e[m:\e[31m\w\n\e[33m$(__git_ps1 "(%s) ")\e[m$ \e[m'
